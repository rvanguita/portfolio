source "https://rubygems.org"

gem "github-pages", "~> 232", group: :jekyll_plugins
gem "webrick", "~> 1.8"

# Ruby 3.4+ moved these out of the default gems, but the pinned Jekyll 3.x
# toolchain (via github-pages) still expects them to be loadable.
gem "erb"
gem "csv"
gem "base64"
