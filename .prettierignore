# Build / gerado / lockfile
dist/
.astro/
node_modules/
package-lock.json
report.*.json

# Servido verbatim
public/

# CSS afinado à mão (alinhamento de colunas, arte em comentário)
src/styles/

# Entradas numa linha só, de propósito
src/data/certificates.ts

# Markup sensível a espaço em branco (<span>{expr}</span>, <b>{n}</b>{texto})
# — o Prettier injeta whitespace que muda o render.
src/components/panels/Channels.astro
src/pages/certificacoes.astro
