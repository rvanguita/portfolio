#!/usr/bin/env bash
# Pre-commit hook: roda a suíte de testes do portfólio antes de permitir o commit.
# Ativado uma vez por clone com: git config core.hooksPath .githooks
set -e

echo "🧪 Rodando scripts/test_site.py antes do commit..."
python3 scripts/test_site.py
