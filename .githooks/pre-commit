#!/usr/bin/env bash
# Pre-commit hook: valida o app Next antes de permitir o commit.
# Ativado uma vez por clone com: git config core.hooksPath .githooks
set -e

echo "🎨 ESLint..."
npm run lint

echo "🔎 Typecheck..."
npm run typecheck

echo "🧪 Testes (Vitest)..."
npm test
